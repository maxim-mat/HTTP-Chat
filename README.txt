Python based HTTP chat server (PyChat for short).

This program runs async HTTP server that provides chat services.

To run (on any platform):

in cmd or other terminal:

cd [location of HTTP-Chat]
python server.py
